services:
  proxyforge:
    image: ghcr.io/masterdeepak15/nginxproxyforge:latest
    container_name: proxyforge
    restart: unless-stopped
    ports:
      - "80:80"     # proxied HTTP traffic (real nginx)
      - "443:443"   # proxied HTTPS traffic (real nginx)
      - "81:81"     # admin dashboard (UI + API), same convention as Nginx Proxy Manager
    volumes:
      - ./data:/data
    environment:
      # Set both before first boot in production. Left as-is, the account
      # is created as admin@proxyforge.local / changeme (a known default,
      # forced to change on first login) - fine for local testing, not for
      # anything reachable outside localhost.
      - ADMIN_EMAIL=admin@proxyforge.local
      - ADMIN_INITIAL_PASSWORD=changeme
      - JWT_SECRET=ProxyForge_JWT_Signing_Key_2026_Secure_v1_X9kP4mL7qR2tH8nW

  # To build locally from source instead of pulling the registry image, swap
  # the `image:` line above for:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
