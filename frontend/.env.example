# Only needed when running the frontend's own Vite dev server (`bun run dev`)
# against a backend running separately (`cd backend && npm run dev`).
# In production (inside the Docker container) the frontend talks to `/api`
# on the same origin, proxied by the container's nginx — this var is unused there.
VITE_API_BASE_URL=http://localhost:3001/api
